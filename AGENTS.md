# AGENTS

## Validation Requirement

- After applying any code change in this repository, always validate it by building the node package with `npm run build`.
- Treat the build as the default validation step for edits unless the user explicitly asks for a different validation flow.
- Do not consider a change complete until the build has succeeded or a concrete build blocker has been reported.
